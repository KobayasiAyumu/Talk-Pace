Talk Pace (面接練習用・早口チェッカー)

**アピールポイント:** 最新Web APIの活用、リアルタイム処理、課題解決力

```markdown
# Talk Pace - Interview Coach 🎤

就活生の悩みである「面接での早口」を矯正するためのトレーニングツールです。
Web Speech APIを活用し、話すスピード（文字数/分）をリアルタイムで計測・可視化します。

**👉 デモサイトまだ実装していません

## 💡 開発背景 (Background)
面接練習の際、緊張して早口になってしまい、聞き取りづらいと指摘されることがありました。
客観的な数値（1分間に何文字話しているか）が見えればペース配分ができると考え、リアルタイムでフィードバックが得られるツールを作成しました。

## ✨ 特徴 (Features)
* **Real-time Analysis**: 音声認識AIが文字起こしを行うと同時に、発話速度（CPM: Characters Per Minute）を計算します。
* **Visual Feedback**: スピードメーターのアニメーションにより、「遅い」「適切」「早い」を直感的に判定できます。
* **Robust Error Handling**: ブラウザの音声認識が途切れた際に自動再接続を行うなど、実用性を重視したエラーハンドリングを実装しました。

## 🛠 使用技術 (Tech Stack)
* **Web Speech API** (`SpeechRecognition`): 音声の文字変換
* JavaScript (Vanilla JS): 速度計算アルゴリズム
* CSS3: メーターの動的アニメーション (`conic-gradient`, `transform`)

## 🚀 技術的なこだわり
### 音声認識の継続性確保
Web Speech APIは無音が続くと自動停止する仕様がありますが、面接練習中に止まらないよう、`onend` イベントフックで自動再開させるロジックを実装しました。

```javascript
recognition.onend = () => {
    if (isRecording) {
        recognition.start(); // 粘り強く再開
    }
};
```

Author[AYUMU]
